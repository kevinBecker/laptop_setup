{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++ build active file",
            "command": "${cwd}/build.sh",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        },

        {
            "label": "Run_lab_05",
            "options": {
                "cwd": "${workspaceFolder}/missions/lab_05/alpha"
            },
            "command": "./launch.sh",
            "args": [""],
            "type": "shell"
        }
    ],
    "version": "2.0.0"
}
